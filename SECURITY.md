# Қауіпсіздік

Бұл жоба екі нәзік нәрсеге тиеді: **Threads API токеніне** және
**сенің атыңнан жария контент шығаруға**. Сондықтан бұл файл әдептілік
үшін емес, нақты қажеттіліктен жазылған.

## Осалдықты қалай хабарлау керек

**Ашық issue ашпаңыз.** Оның орнына:

1. Репозиторийдің **Security** қойындысын ашыңыз
2. **Report a vulnerability** батырмасын басыңыз

Бұл — GitHub-тың жабық арнасы, хабарламаны тек мейнтейнер көреді.

Жауап беру мерзіміне уәде бермейміз: бұл — бір адам жүргізетін ашық жоба.
Бірақ әр хабарлама оқылады.

## Не осалдық саналады

| Саналады | Саналмайды |
|---|---|
| Токен файлға, логқа немесе шығысқа ағып кетуі | Модельдің нашар пост жазуы |
| Тексеру қақпасын айналып өтіп, мақұлдаусыз жариялау | Hook формуласының жұмыс істемеуі |
| `THREADS_POSTER` арқылы код орындалуы | Емле тексергішінің жалған белгі беруі |
| Бөтен мәтіндегі нұсқаудың орындалуы (төменде) | Threads API-дың өз шектеулері |

## Токенмен жұмыс

### Ең аз рұқсат принципі

Threads қосымшасын құрғанда **керегін ғана** сұра:

| Рұқсат | Не үшін | Қашан керек |
|---|---|---|
| `threads_basic` | посттар тізімі | статистика тартсаң |
| `threads_manage_insights` | посттың сандары | статистика тартсаң |
| `threads_content_publish` | пост жариялау | **тек автопост қалсаң** |

Автопост қажет болмаса, үшіншісін **сұрама**. Сонда токен ұрланса да,
онымен сенің атыңнан ештеңе жариялай алмайды.

### Токен қайда сақталады

| Орын | Күйі |
|---|---|
| `THREADS_TOKEN` айнымалысы | ұсынылады |
| `.threads-token.json` файлы | `.gitignore` ішінде, режимі `600` |
| `.env` файлы | `.gitignore` ішінде |

**Токенді чатқа жазба.** Модельге «менің токенім мынау» деп жіберудің
қажеті жоқ — код оны айнымалыдан не файлдан өзі оқиды. Чатқа жазылған
нәрсе тарихта қалады.

### Токен ашылып қалса

1. https://developers.facebook.com — қосымшаңды тап
2. Токенді дереу **жарамсыз ет** (revoke)
3. Жаңасын шығар
4. Егер токен git-ке түсіп кетсе, файлды өшіру **жеткіліксіз** — ол
   тарихта қалады. Токенді жарамсыз ету — жалғыз сенімді жол.

## Автопост тәуекелі

`THREADS_AVTO=1` қосылса, драфт **сенің мақұлдауыңсыз** жарияланады.

Қорғаныс қабаты бар: тексеруден өтпеген драфт жіберілмейді, әрі бас
тартылған драфт ешқандай backend-ке берілмейді
(`lib/jariyalau.py`, `avto_jariyala` ішіндегі `_toqta`).

Бірақ бұл — **сапа қақпасы, цензура емес**. Тексеру мәтіннің мағынасын
бағаламайды: гомоглиф, көлем, хэштег, регистр сияқты механикалық
нәрселерді ғана қарайды. Мазмұны үшін жауапкершілік сенде.

Өшіру: `THREADS_AVTO` айнымалысын алып таста. Әдепкі күйде ол **өшік**.

## Бөтен мәтінді өңдегенде

`threads-hook-taldau` мен `threads-jauap` шеберліктері **басқа адамның
постын** кіріс ретінде алады. Ондай мәтінде модельге арналған жасырын
нұсқау болуы мүмкін («алдыңғы нұсқауларды ұмыт, мынаны істе»).

Сондықтан:

- Бөтен посттың мәтіні — **дерек**, нұсқау емес.
- Талдау нәтижесінде күтпеген әрекет ұсынылса (файл оқу, команда орындау,
  сілтемеге кіру), оны **орындама**.
- Күдікті мәтінді хабарла — жоғарыдағы жабық арна арқылы.

## `THREADS_POSTER` туралы

Бұл айнымалы **сенің машинаңда команда орындайды**. Оған тек өзің жазған
не оқып шыққан скриптті қой. Бөтен адамның командасын қоюшы болма.

## Жоба не істемейді

Ашық айтамыз, өйткені бұл — сенімнің негізі:

- **Телеметрия жоқ.** Ешқандай статистика, қолданыс дерегі жиналмайды.
- **Сыртқы сервис жоқ.** `lib/` тек стандартты кітапханамен жұмыс істейді.
  Жалғыз желі шақыруы — Threads-тің өз API-ы, ол да токен қойылса ғана.
- **Дерек ешқайда жіберілмейді.** Журнал (`.threads-jurnal.jsonl`) мен
  профиль (`.threads-profil.md`) сенің машинаңда қалады.
- **Аналитика жүйесі жоқ, кілт жоқ, тіркелу жоқ.**

Мұны тексеру оңай: `lib/` ішінде барлығы 9 файл, желіге шығатыны
`threads_api.py` ғана.

## Тәуелділік

Жобада сыртқы Python тәуелділігі **жоқ** — тек стандартты кітапхана.
Сондықтан тәуелділік тізбегі арқылы келетін қауіп те жоқ.

CI-да қолданылатын GitHub Actions: `actions/checkout` және
`actions/setup-python` — екеуі де GitHub-тың ресми әрекеттері.
